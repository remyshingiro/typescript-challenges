// # Create a Player Profile

// **Difficulty Level:** 1 (Beginner)

// ## Concepts Practiced

// - `string`
// - `number`
// - `boolean`
// - explicit type annotations
// - `console.log()` output

// ## Short Description

// Create a few variables using basic TypeScript types, then print them in the correct order.

// ## Task Instructions

// 1. Create a `playerName` variable with the explicit type `string` and set it to `Riven`.
// 2. Create a `playerLevel` variable with the explicit type `number` and set it to `7`.
// 3. Create a `isPremiumMember` variable with the explicit type `boolean` and set it to `true`.
// 4. Print the three variables using [`console.log()`](https://developer.mozilla.org/en-US/docs/Web/API/Console/log), one per line.
// 5. Print them in this exact order:
//     1. `playerName`
//     2. `playerLevel`
//     3. `isPremiumMember`

// ## Starter Code (`main.ts`)

// ```ts
// // TODO: create your variables here

// // TODO: print the variables here
// ```
// ## Expected Output

// ```text
// Riven
// 7
// true
// ```

// ## Constraints

// - Use explicit type annotations for every variable.
// - Only modify `main.ts`.
// - Output order must match exactly.

let playerName:string = "Riven";
let playerLevel: number = 7;
let isPremiumMember: boolean = true;
console.log(playerName)
console.log(playerLevel)
console.log(isPremiumMember)
